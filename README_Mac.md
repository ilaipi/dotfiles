# Requirements

```
# neovim
brew install neovim
# neovim
pip3 install pynvim

# if has error: LanguageClient-neovim build failed
# should: cd .cache/vimfiles/repos/github.com/autozimu/LanguageClient-neovim
# run: bash install.sh
# if has error: cargo is not available. Abort
brew install rust
```
